# TODO List after server reset

- Compile the new table for the paper
- Update the images in the paper with the new plot aesthetics (look at the prof notebook outputs)
- Check the paper for any inconsistencies after the changes

- Create a stacking Esnemble model with the 2 level meta learner.
  - we use the test set split in half, one for training the meta learner and the other for testing the final ensemble model
  - we can use the same base models as before (start with 6 best variants of out 6 models) and then we can add more models if needed
